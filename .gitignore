# Godot editor/import cache — regenerated by `godot --headless --import`
threesewers/.godot/
*.tmp
.DS_Store

# Python bytecode from the art generators
__pycache__/
*.pyc
node_modules/

# Captured frames staged for critics — derived data, regenerate with
# threesewers/tools/capture.sh rather than storing it
workbench/critique/
